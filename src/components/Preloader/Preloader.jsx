import { LoaderContainer, Loader } from "./Preloader_components"

export const Preloader = () => {
    return (
        <LoaderContainer>
            <Loader></Loader>
        </LoaderContainer>
    )
}